#include <iostream>
#include <string>
#include <vector>
#include "Real.h"
using namespace std;

void sumar(double);
void restar(double);
void agregar(vector<Real>&);
void dividir(double);
void multiplicar(double);

int main() {
	vector<Real> banco;
	return 0;
}
